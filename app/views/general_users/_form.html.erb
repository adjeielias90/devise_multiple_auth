<%= form_with(model: general_user, local: true) do |form| %>
  <% if general_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(general_user.errors.count, "error") %> prohibited this general_user from being saved:</h2>

      <ul>
      <% general_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_field :username %>
  </div>


  <div class="field">
    <%= form.label :password %>
    <%= form.password_field :password %>
  </div>




  <div class="field">
    <%= form.label :provider_id %>
     <p>
       <%= form.collection_select(:provider_id,
                               Provider.all,
                               :id,
                               :name,
                               { :prompt => true })
       %>
     </p>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
